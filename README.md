# ootheca
A reversible glitch art generator, relies on color changes within the file, so ootheca should be used for Jpegs not PNGs.

To use ootheca, run the ootheca.py script inside any terminal.

*Yes, the code is bad, but it still looks somewhat cool so it's fine.*
